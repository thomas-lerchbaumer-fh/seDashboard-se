import {
    SAVE_DASH,
    UPDATE_DASH,
    LOAD_DASH
} from '../types'

export default (state, action) =>{
/*     switch(action.type){

        case LOAD_DASH:
            return{
                ...state,
                ayout: action.payload[0].layout,
                id: action.payload[0]._id
            }
        case SAVE_DASH:
            console.log(action.payload, 'at saving')
            return{
                ...state,
                layout: action.payload.layout,
                id: action.payload._id
            }

        case UPDATE_DASH:
           console.log('in reducer?', action.payload.layout)
            return{
                ...state,
                layout: action.payload.layout,
                id: action.payload._id
            }
        default:
            return state;

    }
 */

}